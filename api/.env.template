# Environment Configuration Template
# Copy this to .env in the api/ directory and fill in your values

# ==================== Google Cloud Platform (GCP) Configuration ====================
# Required: Set your GCP project details
GCP_PROJECT_ID=your-gcp-project-id
GCS_BUCKET_NAME=your-gcs-bucket-name

# Optional: Path to service account JSON key file
# If not set, will use Application Default Credentials (gcloud auth application-default login)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/service-account-key.json

# ==================== Redis Configuration ====================
# Redis is optional for caching
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# ==================== Application Configuration ====================
# API server settings
API_HOST=0.0.0.0
API_PORT=8000
API_WORKERS=4

# ==================== Logging Configuration ====================
LOG_LEVEL=INFO
LOG_FILE=logs/tax_agent.log
AUDIT_LOG_FILE=logs/audit.log

# ==================== Security Configuration ====================
# Temporary file cleanup (in seconds)
TEMP_FILE_CLEANUP_INTERVAL=3600

# Maximum file size for uploads (in MB)
MAX_UPLOAD_SIZE_MB=5

# ==================== Tesseract OCR Configuration ====================
# Path to Tesseract executable (if not in system PATH)
# TESSERACT_PATH=/usr/bin/tesseract
# or on macOS
# TESSERACT_PATH=/usr/local/bin/tesseract
# or on Windows
# TESSERACT_PATH=C:\\Program Files\\Tesseract-OCR\\tesseract.exe

# ==================== Database Configuration (Future) ====================
# DATABASE_URL=postgresql://user:password@localhost:5432/tax_agent
# DATABASE_POOL_SIZE=10

# ==================== Email Configuration (Future) ====================
# SMTP_SERVER=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password
# FROM_EMAIL=noreply@taxagent.com

# ==================== Development ====================
DEBUG=False
ENVIRONMENT=production

# Note: For development, set DEBUG=True and ENVIRONMENT=development
